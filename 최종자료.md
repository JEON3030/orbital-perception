# 최종 자료 — 탐지 + 시맨틱 통합 인식 프로그램 (Orbital Perception)

> **한 줄 요약.** 위성 온보드(Jetson Orin Nano)에서 **YOLO11 객체탐지**와
> **SegFormer 시맨틱 세그멘테이션**을 **한 화면에 겹쳐** 보여주고, 그 추론의
> **프레임당 에너지(mJ/frame)** 까지 실측하는 프로그램. 이 문서는 그 통합 프로그램을
> 중심으로 코드·실행법·실측 결과·산출물·참고자료를 한곳에 모은 최종 자료다.
>
> 작성일 2026-08-05.

---

## 1. 무엇을 합쳐서 보여주는가

YOLO(COCO)는 사람·차·배 같은 **개별 객체(what/where)** 를 잡지만 도로·건물·식생·
하늘·수면 같은 **장면 의미(scene)** 는 못 잡는다. 위성·항공 관점에선 그 장면 의미가 핵심.
그래서 두 인식을 **한 이미지에 통합**한다:

```
입력 사진
  ├─ SegFormer 시맨틱 세그 → 픽셀 전체를 장면 클래스로 색 오버레이(base)
  └─ YOLO11 탐지(.pt)      → 그 위에 객체 박스 얹기(res.plot)
                            = 탐지 + 시맨틱 통합 1장 (combined)
```

- 시맨틱 색 오버레이 위에 YOLO 박스를 그려 **"이 도로 위에 이 차"** 처럼 객체와 배경을
  동시에 읽는다.
- 사진은 속도보다 **정확도 우선** → 640 엔진 대신 `.pt` 모델을 **고해상도(imgsz 1280)
  + TTA(augment)** 로 돌려 작은 객체 인식률을 올린다.

---

## 2. 핵심 프로그램 파일

| 파일 | 역할 | 핵심 위치 |
|---|---|---|
| **`webviz.py`** | **통합 프로그램 본체.** Gradio 웹앱. 사진 업로드 → 탐지+시맨틱 한 장 출력 | `run_image()` (206–258행) — 시맨틱 overlay(`base`) 위 YOLO `res.plot` → `combined` |
| `semantic.py` | SegFormer 시맨틱 세그(road=Cityscapes 19클래스 / aerial=ADE20K 150클래스) + 색 오버레이 | `infer_seg()`, `overlay()`, `class_summary()` |
| `perception.py` | YOLO11 탐지/인스턴스세그 + 전력계측. torchvision NMS 스텁 주입 | — |
| `device.py` | 젯슨 GPU / 노트북 CPU 장치 결정 한 곳. fp16, CUDA sync 시간보정 | `sync()`, `set_mode()`, `summary()` |
| `metrics.py` | mIoU(혼동행렬) + 통합지표 **mIoU-per-Joule** | `--selftest`, `eval_folder()` |
| `powerlog.py` | tegrastats VDD_IN 전력 샘플러(사다리꼴 적분). INA3221 폴백 | `PowerMeter` |
| `export_trt.py` | YOLO .pt → TensorRT FP16/INT8 엔진 | — |
| `webviz.sh` / `run.sh` / `semantic.sh` | NV torch 환경변수 래퍼 | — |
| `deploy/webviz.service` | systemd 상시 구동 유닛 | — |

통합 로직 요지 (`webviz.py:run_image`):
```python
base     = semantic.overlay(bgr, seg, domain, id2label)  # 시맨틱 색 오버레이(BGR)
combined = res.plot(img=base.copy())                     # 그 위에 YOLO 탐지 박스
```

---

## 3. 실행법

```bash
# 통합 시각화 웹앱 (대시보드 + 라이브: 사진 = 탐지+시맨틱 동시, GPU/CPU)
./webviz.sh                       # http://<보드IP>:7860 → "라이브 추론" 탭에서 사진 업로드
                                  #   도메인(road/aerial)·장치(GPU/CPU) 선택 → 통합 1장 출력

# 개별 실행
./run.sh input/sample.jpg                 # YOLO 탐지 + 에너지 리포트
./semantic.sh input/sample.jpg            # 시맨틱(road)
./semantic.sh input/x.png --domain aerial # 시맨틱(aerial)

# 통합 지표
python metrics.py --selftest                                          # 수식 검증
python metrics.py --images <img폴더> --labels <라벨PNG폴더> --domain road
```

systemd로 웹앱 상시 구동 중(`deploy/webviz.service`).

---

## 4. 실측 결과 (전부 이 Jetson Orin Nano, 7W, 추정치 없음)

### 4.1 시맨틱 세그 — 온보드 GPU vs 노트북 CPU (SegFormer-B0, sample.jpg)
| | 젯슨 GPU (fp16, 온보드) | 노트북 CPU (시연) |
|---|---:|---:|
| 속도 | **7.4 FPS** | 0.38 FPS |
| 총 에너지 | **776–927 mJ/frame** | 16,110 mJ/frame |
| 효율 | ~1.08 frames/J | 0.06 frames/J |

→ **온보드 GPU가 노트북 CPU 대비 약 17배 에너지 효율.**

### 4.2 YOLO TensorRT FP16 vs PyTorch (7W)
| task | PT mJ/frame | Engine mJ/frame | 개선 |
|---|---:|---:|---:|
| detect | 429.9 | **283.1** | −34% (순수 −51%) |
| segment | 619.0 | **436.5** | −29% (순수 −45%) |

### 4.3 통합 지표 mIoU-per-Joule (예시)
mIoU 0.650 가정, 순수추론 222 mJ → **약 3.98 mIoU·J⁻¹**
(실제 mIoU는 라벨셋으로 `metrics.py` 산출.)

---

## 5. 산출물 (outputs/)

| 파일 | 내용 |
|---|---|
| `sample_detect.jpg` / `_energy.json` | YOLO 탐지 결과 + 에너지 |
| `sample_segment.jpg` / `_energy.json` | YOLO 인스턴스세그 결과 + 에너지 |
| `sample_semantic_road.jpg` / `_energy.json` | SegFormer 시맨틱(road) + 에너지 |
| `webviz_semantic_road.mp4` | 시맨틱(road) 영상 데모 |
| `webviz_semantic_aerial.mp4` | 시맨틱(aerial) 영상 데모 |
| `webviz_detect_engine.mp4` | 탐지(TensorRT 엔진) 영상 데모 |
| `webviz_segment_engine.mp4` | 세그(엔진) 영상 데모 |
| `trt_compare/` | TensorRT FP16 vs PT 비교 |

---

## 6. 참고 문서 / 논문

- `README.md` — 프로젝트 전체 개요(전력 관점·실측표·환경 메모·다음 단계).
- `docs/보완_정리.md` — 전력우선 → 정확도/의미인식 흡수 확장 경위, 구현 함정·해결.
- (관련 캡스톤 비교) `park-hyun-su/syntax_inference` — 정확도/시각화 반쪽.

---

## 7. 환경 메모 (재현용)

- venv는 `~/wildfire-seg/.venv`(NV torch 2.5, CUDA True) 재사용. ultralytics 8.4.106.
- torchvision 바이너리 없음 → `perception.py`가 NMS만 스텁 주입(+`__spec__` 부여),
  `semantic.py`가 `_torchvision_available=False`로 못박아 SegFormer와 한 프로세스 공존.
- transformers는 전용 `_deps/`(transformers 4.46 + hf-hub 0.26 + tokenizers 0.20)를
  sys.path 앞에 격리 → 다른 프로젝트 안 건드림. torch/GPU는 공유 venv 사용.

---

## 8. 남은 레버 (다음 단계)

- aerial 도메인을 실제 위성/항공 이미지로 검증.
- CamVid 등 라벨셋으로 진짜 mIoU-per-Joule 수치 산출.
- SegFormer도 TensorRT FP16 변환 → 시맨틱 mJ/frame 추가 절감.
- wildfire U-Net(Sentinel-2 12밴드) 온보드 추론 에너지 프로파일 연결.
